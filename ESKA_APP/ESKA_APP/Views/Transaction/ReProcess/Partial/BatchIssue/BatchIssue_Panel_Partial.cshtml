@Html.DevExpress().PopupControl(settings =>
{
    settings.Name = "popupBatchIssue";
    settings.CallbackRouteValues = new { Controller = "ReProcess", Action = "ViewIssueBatch_PopupListOnDemandPartial" };
    settings.CloseAction = CloseAction.CloseButton;

    settings.PopupVerticalAlign = PopupVerticalAlign.WindowCenter;
    settings.PopupHorizontalAlign = PopupHorizontalAlign.WindowCenter;
    settings.Modal = true;

    settings.ShowFooter = true;
    settings.AllowDragging = true;
    settings.AllowResize = false;
    settings.ShowRefreshButton = true;
    settings.FooterText = "";
    settings.Styles.Footer.Paddings.Padding = 0;

    settings.ShowPageScrollbarWhenModal = true;
    
    settings.EnableHotTrack = true;

    settings.CloseAnimationType = AnimationType.Fade;
    settings.PopupAnimationType = AnimationType.Fade;

    settings.LoadContentViaCallback = LoadContentViaCallback.OnFirstShow;
    settings.ClientSideEvents.EndCallback = "popupIssueBatch_EndCallback"; //popupItemTag_EndCallback
    settings.PopupElementID = "ShowButton";
    settings.HeaderText = "Issue Item Batch List";

    settings.SetContent(() =>
    {
        Html.RenderPartial("Partial/BatchIssue/BatchIssue_Form_Partial");

    });

    settings.PreRender = (sender, e) =>
    {
        ASPxPopupControl popup = (ASPxPopupControl)sender; 
        popup.Width = 620;
        popup.Height = 200;
    };


}).GetHtml()