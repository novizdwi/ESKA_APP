@using (Ajax.BeginForm("mantap", "ReProcess", null,
new AjaxOptions
{
    HttpMethod = "POST",
    OnBegin = "OnBegin",
    OnSuccess = "OnSuccessMsgBox",
    OnFailure = "OnFailure",
    OnComplete = "OnComplete",
    UpdateTargetId = "Scale_contentForm",
    InsertionMode = InsertionMode.Replace,

},
new
{
    id = "Scale_formDetail",
    @class = "edit_form",
    style = "height: 200px; width: 100%;",
    autocomplete = "off"
}))
{
    <div id="Scale_contentForm">
        @Html.Partial("Partial/Scale/Scale_Panel_Partial")

    </div>
}
