@using (Ajax.BeginForm("mantap", "ProductionTaskActivity", null,
new AjaxOptions
{
    HttpMethod = "POST",
    OnBegin = "OnBegin",
    OnSuccess = "OnSuccessMsgBox",
    OnFailure = "OnFailure",
    OnComplete = "OnComplete",
    UpdateTargetId = "Pause_contentForm",
    InsertionMode = InsertionMode.Replace,

},
new
{
    id = "Pause_formDetail",
    @class = "edit_form",
    style = "height: 200px; width: 100%;",
    autocomplete = "off"
}))
{
    <div id="Pause_contentForm">
        @Html.Partial("Partial/Pause/Pause_Panel_Partial")

    </div>
}
